repos:
  - repo: local
    hooks:
      - id: pytest-tests
        name: Run pytest on tests directory
        entry: pytest tests
        language: system
        types: [python]
        always_run: true
        pass_filenames: false

